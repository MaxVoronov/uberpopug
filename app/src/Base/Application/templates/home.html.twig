{% extends 'layout/base.html.twig' %}

{% block title %}Панель управления :: {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-body">
                    <a href="#intoCollapse" class="btn btn-primary" data-toggle="collapse" role="button" aria-expanded="false" aria-controls="intoCollapse">Инструкция по стартовой настройке административной панели</a>
                    <div class="collapse into-collapse mt-3" id="intoCollapse">
                        Hello!<br>
                        {{ homePage }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
